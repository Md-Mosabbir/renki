Use while a ride search is open. It draws the matcher expanding a ring of cells around your destination, so it means something a spinner would not.

```jsx
<SearchingRings label="Looking for riders" sublabel="Leaving around 6:30 PM" />
```
