Use for the meet-and-scan moment (confirming a friend, starting a ride). Visual stand-in for the product's real QR symbol.

```jsx
<CodePlate code="RNK-4T2Q" caption="Expires in 90s" />
```

Never tint, animate or round it — a camera has to read it. Expressive motion belongs to the blob around it, not the plate.
