import * as React from 'react';

/**
 * The searching state: rings expanding outward from the mark.
 */
export interface SearchingRingsProps {
  label: string;
  sublabel?: string;
  className?: string;
}

export declare function SearchingRings(props: SearchingRingsProps): React.ReactElement;
