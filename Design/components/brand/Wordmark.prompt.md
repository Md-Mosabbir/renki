The Renki lockup — use it wherever the brand signs a screen: app headers, sidebars, onboarding, sign-in.

```jsx
<Wordmark />
<Wordmark tone="inverse" size="lg" />
```

The square is the logo. Never round it, never gradient it, never put anything else in its place. On a dark panel pass `tone="inverse"` — only the text flips.
