Use once the route's layout is known. A sheen, never a pulse — a pulse competes with the content it replaces.

```jsx
<Skeleton width="40%" height={10} />
<SkeletonList rows={4} />
```

Also exports `SkeletonList`.
