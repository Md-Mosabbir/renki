For content that floats over a screen it does not replace: matches over a map, filters over a search. Absolutely positioned; give the parent `position: relative`.

```jsx
<Sheet title="Two people are going your way" onClose={close}>…</Sheet>
```
