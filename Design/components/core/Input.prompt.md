Standard text field. Pair with `Label`.

```jsx
<Label htmlFor="phone">Phone</Label>
<Input id="phone" placeholder="01XXXXXXXXX" />
```

`size="lg"` (44px) for onboarding forms, where fields are the whole screen.
