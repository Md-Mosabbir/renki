The stranger-match deck. Facts are labelled with the eyebrow style; the note at the bottom must keep saying that yes is not a booking.

```jsx
<SwipeCard name="Imran Kabir" badge={{ label: 'Wants to ride with you', accepted: true }} facts={facts} intent="yes" offset={40} />
```
