Render inside a `<ul>`. A friend, a request and a search result are the same row with different actions.

```jsx
<FriendRow name="Sadia Rahman" note="Met 3 Mar">
  <Button size="sm" variant="outline">Remove</Button>
</FriendRow>
```
