Use for a fork between genuinely different products — a stranger match versus a friends group. Each option gets a sentence of what it actually is.

```jsx
<RideOption icon={<i data-lucide="search" />} title="Match with a stranger" body="One other rider leaving campus around the same time. You both swipe; a ride happens only if you both say yes." />
```
